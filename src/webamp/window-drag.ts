/**
 * No-op: Tauri window is fullscreen transparent canvas.
 * Webamp handles its own internal window dragging.
 */
export function setupWindowDrag() {}

/**
 * No-op: window starts fullscreen via config.
 */
export function setupFullscreenCanvas() {}
